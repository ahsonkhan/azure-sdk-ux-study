#include <stdio.h>
#include <string>

// Add the necessary #include headers here

// Run this application that is using the C++ Azure Storage SDK to copy a large blob and track progress by 
// printing a timer, i.e. "not done <number of seconds since the operation started>" message to the console every second \
// the copy operation is in progress.
// Note: The necessary azure-storage-blobs dependency has already been installed for you.
// Note: Here are the Azure Storage account details. The connection string to connect to the service is set in the environment variable.
void CopyWithProgress()
{

}

int main()
{
    CopyWithProgress();
    puts ("Task 4 complete");
    return 0;
}
